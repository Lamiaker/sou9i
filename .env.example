# 🌐 APPLICATION
# L'URL de votre application (ex: https://votre-domaine.com ou http://localhost:3000)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# 🔑 AUTHENTIFICATION (NextAuth)
# URL de base pour l'auth (même que APP_URL)
NEXTAUTH_URL="http://localhost:3000"
# Générer un secret robuste : openssl rand -base64 32
NEXTAUTH_SECRET="votre_secret_tres_long_et_securise"

# 💾 BASE DE DONNÉES (PostgreSQL)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://user:password@localhost:5432/sweetlook"

# 📧 EMAILS (Resend)
# Clé API obtenue sur resend.com
RESEND_API_KEY="re_xxxxxxxxxxxx"
# Email expéditeur valide (doit être configuré sur Resend)
EMAIL_FROM="SweetLook <noreply@votre-domaine.com>"

# 🛡️ CAPTCHA (Cloudflare Turnstile)
# Clé publique (visible dans le client)
NEXT_PUBLIC_TURNSTILE_SITE_KEY="0x4xxxxxxxxxxxxxxxxxxxxxxx"
# Clé privée (gardée secrète côté serveur)
TURNSTILE_SECRET_KEY="0x4xxxxxxxxxxxxxxxxxxxxxxx"

# ⚡ REDIS (Rate Limiting & Caching)
# Requis en production
REDIS_HOST="localhost"
REDIS_PORT="6379"
REDIS_PASSWORD=""

# 💬 MESSAGERIE (Socket.io)
# Origines autorisées pour les WebSockets (votre domaine)
SOCKET_IO_CORS_ORIGIN="http://localhost:3000"

# 🛠️ DÉVELOPPEMENT / PRODUCTION
NODE_ENV="development"
